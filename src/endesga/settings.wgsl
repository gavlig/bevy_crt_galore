#define_import_path bevy_crt_galore::endesga

struct CrtSettings {
	aberration_amount	: f32,
	noise_amount		: f32,
	vignette_amount		: f32,
	rounded_amount		: f32,
	pixelate_amount		: f32,
	mask_amount			: f32,
	distortion_amount	: f32,
	glow_amount			: f32,
}